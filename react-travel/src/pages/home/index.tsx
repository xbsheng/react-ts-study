import React from 'react'
import { Button } from 'antd'

interface Props {
  
}

const Home:React.FC = (props: Props) => {
  return (
    <div>
      <Button>按钮</Button>
      <h2>Home</h2>
    </div>
  )
}

export default Home
